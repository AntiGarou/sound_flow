name: soundflow_workspace
description: SoundFlow Monorepo Workspace
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ">=3.5.0 <4.0.0"

dev_dependencies:
  melos: ^6.1.0
  very_good_analysis: ^6.0.0
